# REQ-<task-id>

> 快速卡：每项填一句，越具体越好。填完即可 `agent-os run <task-id>`。

- 版本基线：<base-version>
- 领域：<领域>（如 sales/website/data/seo）
- 目标：<目标>（一句话 + 可量化验收，如"把 X 转成 Y 报告，错误率<1%"）
- 范围：
  - 必做：
  - 不做：
  - 后续：
- 输入：<文件路径/格式/表结构/权限>
- 输出：<文件路径/格式/命名>
- 验收：
  - 功能：
  - 质量：rc=0、无 unknown、回归全绿、人工抽检通过
  - 性能/成本：<如单次<10分钟、token 预算>
- 约束：技术栈/环境 / 安全(不写明文密钥) / 合规(审计日志)
- 风险与回滚：失败点 + 保留旧版 + 还原方案
- 是否允许改底座规则：否（改→走 BASE 提案/门禁，不在本任务区直改）
- 自动化程度：全人工 / 半自动(仅草案) / 自动(仅白名单+四锁)
- 观察周期：如跑 3 轮再评估合并
